name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          VERSION=$(npm show @minecraft/server versions --json | jq -r '.[-1]')
          echo "Latest module version: $VERSION"
